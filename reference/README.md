# Reference Archive

This directory preserves the process and reference material from the V1 repository before the codebase reset.

Retained here:

- root markdown documents from V1
- all markdown files previously under `docs/`
- the V1 wrap and AI channel operating model created during the package-wrap phase

The active workspace outside this directory has been intentionally cleared so the rebuild can start from a clean slate while keeping the previous thinking available for lookup.

Data retained separately:

- `.env`
- raw data under `data/raw`
- challenge corpus under `data/challenge_doc_corpus`

Synthetic or generated runtime artifacts are intentionally excluded from the retained workspace.
